# 生产环境配置示例文件
# 请复制此文件为 .env.production 并根据实际部署情况修改

# === 基本配置 ===
NODE_ENV=production

# === 端口配置 ===
FRONTEND_PORT=3000
BACKEND_PORT=3001

# === 重要：API地址配置 ===
# 选择以下配置方案之一：

# 方案1：使用Nginx反向代理（推荐）
# 前端和API都通过同一域名/端口访问，避免跨域问题
NEXT_PUBLIC_API_URL=/api

# 方案2：直接暴露后端端口
# 将 your-server-ip 替换为实际的服务器IP地址
# NEXT_PUBLIC_API_URL=http://your-server-ip:3001/api

# 方案3：使用域名
# NEXT_PUBLIC_API_URL=https://your-domain.com:3001/api

# === 数据库配置 ===
DATABASE_URL=file:/app/data/sqlite.db

# === 安全配置 ===
# 生产环境必须修改为强密码！
JWT_SECRET=your_super_strong_jwt_secret_change_this_now
JWT_EXPIRES_IN=24h

# === 可选：Redis配置 ===
# REDIS_HOST=your-redis-host
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password
# REDIS_URL=redis://:your-redis-password@your-redis-host:6379