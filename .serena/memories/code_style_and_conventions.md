Backend follows MVC: controllers handle HTTP, models handle DB, routes define endpoints, middleware for auth/validation. Frontend uses component-based structure with App Router, hooks for logic, providers for global state. TypeScript strict mode; avoid mock/simplified/temp solutions; implement full error handling, caching/perf, and security validation (per CLAUDE.md). Use production-grade patterns, long-term maintainability.
